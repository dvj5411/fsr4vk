Initial Doom TDA support
Fixed custom Optiscaler to no longer throw weird warnings
upscaler should be self contained now, so a .dll drop in should now be theoretically possible with a rename to amd_fidelityfx_vk.dll
